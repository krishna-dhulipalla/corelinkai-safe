# CLAUDE.md

Read `AGENTS.md` first.

Project docs live under `.agents/`.

Use:

- `.agents/execution-plans/` for active implementation sequencing
- `.agents/progress.md` for major milestone logging
- `.agents/architecture/` for current design and rationale

Claude-specific preference:

- prefer incremental edits
- summarize architectural impact when changing runtime flow
- use scoped reviews/checkpoints instead of broad rewrites
